ThinksCode = React.createClass({
  render() {
    return (
      <section className={classNames('fullPageSection', 'codeSection')}>
      <div>Justin thinks he can code</div>
      </section>
    );
  }
});
